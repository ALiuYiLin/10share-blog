// import fs from 'fs'
// import path from 'path'
// import { blogsDir } from '~~/utils';

export default defineEventHandler(async (event) => {
  // try{
  //   const {content,blogPath,name} = await readBody<{content:string,blogPath:string,name:string}>(event)
  //   const blogPathAbs = path.resolve(blogsDir,blogPath,`${name}.md`) 
  //   fs.writeFileSync(blogPathAbs,content,{
  //     encoding:'utf-8'
  //   })
  // }catch(e){
    
  // }

});